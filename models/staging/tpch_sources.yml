version: 2

sources:
  - name: tpch
    database: sales-demo-project-314714
    schema: raw
    tables:
      - name: orders
        columns:
          - name: o_orderkey
            description: unique record id
            tests:
              - unique
              - not_null